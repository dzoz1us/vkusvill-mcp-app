# Use mock data without a running backend.
VITE_USE_MOCK=true

# To use the real backend, set VITE_USE_MOCK=false. Leave VITE_API_URL empty
# during local development so Vite proxies /api requests to localhost:8000.
VITE_API_URL=
